module main

import x.json2 as json
import x.json2.decoder2

struct Data {
mut:
	a_token ?[]string
	dfv     ?[]struct {
		key string
	}
}

fn main() {
	j := '{
	"a_token2": ["one", "two"],
	"a_email2": {
		"email": "email@email.ru"
		}
	}'
	d := decoder2.decode[Data](j)!
	println(d)
}
